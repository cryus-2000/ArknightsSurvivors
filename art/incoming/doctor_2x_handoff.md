# 博士 @2x 高清待机

`doctor@2x.png`：单帧 96×96，横向 2 帧，总尺寸 192×96，2 fps 循环，脚底锚点 **(48,92)**，正面略朝右。密度 2，绘制倍率按 @2x 通道减半。保留旧 `doctor.png`。

从博士原制作 imagegen 大图 `fceeff3c-90ee-4346-a16a-b225e5129e9a` 重新采样，增加兜帽、面罩、衣缝与徽记细节，二值 alpha、1 新像素 #080E18 外轮廓。

旧 `npc_v10_manifest.json` 的博士锚点为 (24,45)，直接翻倍是 (48,90)。本次按用户指定改为 (48,92)，因此图像整体相对原图 2× 下移 2 新像素；外轮廓形状完全一致，偏移不超过高清约定的 2 新像素。开发侧使用 (48,92)，避免沿用旧清单机械翻倍后的 (48,90)。

已验证尺寸、两帧差异、非空、二值透明、边界留白，以及平移后剪影完全一致；`doctor_2x_manifest.json` 含原图／输出 SHA-256。`doctor_2x_preview.png` 是同显示尺寸的新旧对比。未改游戏代码，未做 Godot 运行验证。
