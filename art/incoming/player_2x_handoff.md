# 水月 96px 动画与术师紫光交接

按 `docs/20_hires_spec.md` 交付水月全部五组 @2x 动画，共 20 帧。以用户提供的水月立绘为服饰校准：深蓝帽、浅蓝发与紫色发梢、黄绿内领、蓝蝴蝶结、白蓝外套、黑短裤、全黑袜、收拢伞。旧版 48px 帧条保留。

| 文件 | 单帧 | 帧数 | fps | 播放 |
|---|---|---|---|---|
| player_idle@2x.png | 96×96 | 4 | 4 | 循环 |
| player_run@2x.png | 96×96 | 6 | 10 | 循环 |
| player_attack_48@2x.png | 96×96 | 4 | 16 | 单次，沿用旧出手时序 |
| player_hurt@2x.png | 96×96 | 2 | 10 | 单次 |
| player_death@2x.png | 96×96 | 4 | 6 | 单次、停末帧 |

统一锚点 `(48,92)`，密度 2。引擎读取 @2x 后将绘制倍率减半，画面中人物大小、位置与判定沿用原图。单帧宽高翻倍，帧数与速度不变；不可再次将画面中的人物放大两倍。未修改游戏代码或可选静态 `player@2x.png`。

制作：使用原 48px 图对应的 imagegen 大图，按原采样变换重新提取 96px 细节，扩展原 24 色调色板的中间色；原图放大两倍的二值透明轮廓作为约束，外缘重画为 1 新像素 #080E18 描边。并非直接最近邻放大。五条的剪影与旧版 2× 剪影完全相同（偏差 0，满足不超过 2 新像素的限制）。清单记录每帧的像素数、相较直接放大的颜色变化量、帧间差异、原图 SHA-256 和输出 SHA-256。

原制作大图 source IDs：待机 `485bbcd1-dae4-4269-9e13-861c368b25d4`；跑步 `a5e0d4a4-3fef-4777-9bae-94aebddf9cb1`；挥伞 `5f640a2c-8e19-4834-81c1-edbd4dde8c0d`；受击 `052f6490-e794-4ecd-91da-04930bcff136`；倒地 `ca752162-82c4-42e4-aa6f-75748a8938d5`。本轮内置 imagegen 的高清重绘提示为：保留原动作、帧数、朝向、轮廓和服饰，增加发丝、眼睛、衣褶、伞骨细节。最终交付采用原制作大图以保持动作对应。

术师：`ally_caster.png` 和 `ally_caster_attack.png` 的青色发光分别修改 24、42 像素，统一为深紫 `#6F4BB5`、紫 `#A885FF`、浅紫 `#DDCCFF`。其余像素、透明度、尺寸和帧数保持不变。`ally_caster_move.png` 无该类发光像素，无需改动。术师的独立弹道和命中特效沿用 main 中 Claude 已制作的紫色版本。本次未修改辅助职业 `ally_support*`。

验收：5 条高清帧条的尺寸、20 帧非空且相邻不重复、二值 alpha、边界留白、原轮廓一致性均通过；人工查看同显示尺寸的新旧对照。`player_2x_preview.png` 为全部帧对照，`player_2x_preview.html` 为内嵌图片的动画对照。Godot 实际显示仍需开发侧接收提交后验证。
