# 27 · 数值平衡框架（P5）

> 2026-09-25 起。目标：在「类幸存者爽游」的前提下，让 8 名干员、各条藏品流派与任意编队组合都能玩、都能赢、但都得动脑。
> 本文是**架构与工作流**；具体数字都在数据文件里（§3），改数不改文档、改文档只改目标与结论。

---

## 1. 体验目标（先定感觉，再定数字）

| 阶段 | 时间 | 玩家应该感觉到 | 数值含义 |
|---|---|---|---|
| 探索 | 0:00–3:30 | 「我一个人也能站住，但队伍在哪？」 | 单人开局任何职业都能撑到第一个 Boss；输出刚好够清怪，不够清屏 |
| 成型 | 3:30–7:00 | 「引擎点着了」：第二、三名干员入队、精一、藏品开始联动 | 输出增速 > 敌人生命增速；大群从「躲」变成「收割」 |
| 掌控 | 7:00–10:00 | 清屏爽感 + 精英 / Boss 是考试 | 小怪一碰就死，但精英 / Boss / 大群里的进化体仍需走位与技能节奏 |
| 大考 | 10:00 | 最终 Boss | 成型的队伍 60–75% 能赢；乱选的队伍 30–40% |

**不能出现的**（框架 §34–36）：某一套编队 / 流派同时最快成型、最强清屏、最强打 Boss、最能生存；每局伤害构成都一样；某个干员选了就等于自杀。

### 1.1 硬指标（`--balance` 机器人，难度 0，≥ 5 seed）

| 指标 | 目标 | 说明 |
|---|---|---|
| 单人开局撑到 3:30 | 每名干员 ≥ 90% | 「任何职业都能开局」（docs/23 §10） |
| 三人编队胜率 | 预设编队 55–75%，任意两两组合 ≥ 35% | 太高 = 无聊，太低 = 挫败 |
| 干员伤害占比（三人队） | 每人 15–50% | 没人是挂件，没人一个人打完 |
| 8 名干员单人 10 分钟平均存活 | 最强 / 最弱 ≤ 1.35 | 全员可玩：差距靠技能表现力，不靠面板 |
| 等级里程碑 | 2:00 ≈ Lv7、5:00 ≈ Lv17、8:00 ≈ Lv26、10:00 ≈ Lv32 | docs/23 §8 |
| 第一次精一 / 精二 | ≈ 1:00–1:40 / 6:00–7:00 | 成型节奏 |
| 次要来源占比（无人机、藏品触发、援护） | 10–20% | 副系统是加分项不是主力 |

---

## 2. 分层模型：谁负责什么

```
玩家感受
 ├─ 敌人压力曲线    data/balance.json  enemy 段   （生命 / 攻击 / 刷怪率随时间；精英 / Boss 倍率）
 ├─ 成长节奏        data/balance.json  xp、levelup 段（经验曲线、卡池概率、招募时机）
 ├─ 干员基础数值    data/characters/<id>.json  base 段（攻击 / 间隔 / 射程 / 技能倍率）
 ├─ 干员档位        data/balance.json  operators 段 （±5% 的 add 修正，写入 op:<id> 作用域）
 ├─ 干员成长线      data/characters/<id>.json  progression（每节点 +15% / +20%，精一精二解锁）
 ├─ 博士 / 全队被动  doctor.gd PASSIVES（上限 4 种，各自叠层）
 ├─ 藏品            data/relics.json + relic_effects.json（流派 / 稀有度 / 权重）
 └─ 难度            data.gd DIFFICULTY（逐级叠加负面，不动基础曲线）
```

**原则**：每一层只调自己的旋钮。发现「维什戴尔太强」→ 先看 `characters/wisadel.json base`，不动 `balance.json enemy`；发现「所有人 7 分钟后都被大群淹」→ 动 `enemy` 曲线，不动任何干员。

---

## 3. 干员：基础数值 + 档位

### 3.1 基础数值（`base` 段）

每名干员脚本里用 `base("key", 默认值)` 读数，JSON 缺键就用代码默认值，所以 JSON 与代码永远同值起步。键名约定：

| 键 | 含义 |
|---|---|
| `atk` | 普攻基础伤害（凯尔希为 `m_atk` = Mon3tr） |
| `cd` | 普攻间隔（秒，被 `op_aspd` 除） |
| `range` / `reach` | 远程索敌距离 / 近战触及半径（被 `op_range` 乘） |
| `aoe` / `aura` / `block_r` | 范围类半径 |
| `s1_* s2_* s3_*` | 各技能倍率 / 数量 / 半径 |
| 其他 | 天赋系数（`quake`、`shade`、`hit_sp`…） |

### 3.2 档位（`balance.json operators`）

按原作口碑分档，但**全员可玩**是硬约束，所以档位只做 ±5% 的 `add` 修正（`op_atk` / `op_skill_power` 等）：

| 档 | 干员 | 修正 | 强在哪（不靠面板） |
|---|---|---|---|
| S+ | 维什戴尔 | 攻 +5%、技能强度 +5% | 三段爆炸 + 残影连锁：清群、打精英都行，是「爽」的代表 |
| S | 艾雅法拉、凯尔希 | +3% / +3% | 火山持续 AoE；真伤 + 治疗双修 |
| A+ | 塞雷娅、铃兰 | 技能强度 +2% | 生存 / 增幅型：让别人变强 |
| A | 水月、推进之王、斯卡蒂 | 0 | 基准 |

档位差异的真正来源是**技能表现力**（连锁、AoE、控制），不是数字。调档位系数只用于最后微调（±2% 一档）。

### 3.3 职业预算（设计时用来估算 base 的参考）

单体 DPS 基准（普攻，未计 1.25 灯火加成）≈ **25–35**：

| 职业角色 | 直接 DPS 预算 | 补偿 |
|---|---|---|
| 主输出（近卫 / 狙击 / 术师 / 特种） | 100% | — |
| 节奏（先锋） | 85% | 全队技力 |
| 保护（重装） | 70% | 阻挡 + 减伤 + 治疗 |
| 治疗（医疗） | 65%（Mon3tr） | 持续治疗 |
| 增幅（辅助） | 65% | 减速 + 易伤 + 全队攻击 |

技能预算：S1 ≈ 3–5 秒普攻伤害；S2 ≈ 8–12 秒；S3 ≈ 15–25 秒或 8–14 秒的形态变化。永久型技能（凯尔希 S2、铃兰 S2）按「一次性 +30% 效率」计。

---

## 4. 敌人压力曲线（`balance.json enemy`）

- 生命：`base × (1 + min(t, hp_knee)/hp_div + max(t − hp_knee, 0)/hp_late_div)` → 2:00 ×2、8:00 ×5、10:00 ×5.4
- 攻击：`base × (1 + min(t, dmg_knee)/dmg_div)` → 8:00 封顶 ×2.85
- 刷怪率：`spawn_base + t/spawn_div` 只/秒；Boss 在场 ×0.8，灯火 < 30 ×1.15
- 精英：生命 ×7、攻击 ×1.3、经验 ×10；Boss：`base × (1 + t/boss_hp_time_div)`
- 难度 1–10：逐级叠加（data.gd），不改曲线形状

**调曲线的判断标准**：所有编队在同一时间点集体死亡 → 曲线太陡；所有编队都在 10:00 前把最终 Boss 打到 0 → 曲线太平。只有一两个编队掉队 → 别动曲线，去看那两个编队。

---

## 5. 流派与编队

藏品流派 2026-09-26 起按 docs/35 分为八条（A 前锋·近战 / B 追击·召唤 / C 控制·技能循环 / D 收割·弱点 / E 远程·火力 / F 深蓝·低灯火 / G 编队·协同 / H 守护·续航），职业专属藏品只在编队有该职业时出现。编队制之后流派由**编队职业组合**决定主体，藏品做放大：

| 组合 | 期望玩法 | 应该赢在 | 应该输在 |
|---|---|---|---|
| 双输出 + 辅助 | 清屏 | 大群、中期 | 灯火低时的 Boss |
| 输出 + 医疗 + 重装 | 站桩 | Boss、生存 | 大群拖太久 |
| 先锋 + 任意 | 技能循环 | 技能型干员成型快 | 面板低 |
| 三近战 | 高风险高回报 | 击退 / 眩晕控场 | 远程精英 |
| 三远程 | 风筝 | 前期 | 被贴身 |

每种组合都应有 ≥ 35% 胜率、不同的伤害构成（§1.1）。

---

## 6. 工作流：怎么测、怎么改

### 6.1 机器人

`--balance` 的机器人（game.gd `_bot_pick`）像一个懂玩的玩家：优先干员深度 / 技能卡，Lv 8 前见招募必招，全队被动按权重选（`balance.json bot`），填充卡只在没得选时拿。`--botrandom` 退回纯随机（用来测「乱选」的下限）。

**分档机器人（docs/29）**：`--bot=afk|bad|normal|expert`，缺省 normal（即上面这个）。批跑 `--bots afk,bad,normal,expert` 一次跑四档，报告按档对照难度目标给出 达标 / 偏难 / 偏易；标准矩阵 `--preset starts`（7 个单人开局、自然招募）。

### 6.2 批跑

```bash
python tools/balance_run.py --preset solo --seeds 5        # 8 名干员单人开局
python tools/balance_run.py --preset squads --seeds 5      # 6 个预设编队
python tools/balance_run.py --preset pairs --seeds 2       # 28 个两人组合（粗筛）
python tools/balance_run.py --squad wisadel,suzuran,saria --seeds 5 --extra "--botrandom"
```

输出到 `build/balance/<tag>_<时间>.md`：胜率、存活、等级里程碑、终 Boss 剩余、精二占比、伤害来源、死因。一局 10 分钟模拟约需 2–4 分钟真实时间，8 并行。

### 6.3 改数流程

1. 跑基线（solo + squads，≥ 5 seed），记录到 §8。
2. 对照 §1.1 硬指标，找**最偏离**的一项。
3. 按 §2 找到该改的层，只改一层、一次改一个方向（±10–15%）。
4. 重跑受影响的矩阵；不受影响的编队应当基本不变（否则改错层了）。
5. 通过后把结论追加到 §8，提交时带上 `build/balance/*.md`。

### 6.4 护栏（改数前后都要看）

- `python tools/check.py` 全部通过（含 `tests/test_core.gd`、每名干员冒烟、同 seed 复现，docs/36）。
- 单人开局每名干员 3:30 存活率不掉。
- 任何一次改动不能让某个编队胜率变化超过 25 个百分点（否则是 bug，不是平衡）。

---

## 7. 已知结构性问题（P5 一并修）

| 问题 | 状态 |
|---|---|
| 无人机只在水月在队时才更新（`mizuki.gd` 调 `_update_weapons`） | **已修**：移到 `game.gd _update`，跟随博士 |
| 机器人在 `--balance` 下纯随机选卡，测出的下限偏低 | **已修**：`_bot_pick` |
| 干员数值写死在 .gd | **已修**：`base` 段 |
| 曲线 / 卡池概率写死在 game.gd | **已修**：`balance.json` |
| 藏品流派引导（RelicDb.STEER / build_profile）没接进游戏 | **已接（简化版）**：`_relic_pool_ids` 按已拿流派件数加权 ×1.3ⁿ（封顶 ×2），见 docs/35 |
| 部分藏品仍只对水月生效（172 / 112 / 208 / 170 / 95 / 96） | **已修**（docs/35）：追击类按伤害标签、荣耀绶带按「不带范围」、极速之手按编队人数、投币玩具写全队攻速 |
| `boss_dmg`、`enemy_dmg_taken` 等属性定义了没人读 | 待清理或接入 |
| 同一 `--seed` 不能完整复现：音效的墙钟限流已修（sfx.gd 私有 RNG），仍在 ≈3:50 后分叉；已排除 `Time.get_ticks`（只用于卡片动画）、`create_timer`（只在 title）、`sort_custom` | 未定位。影响：对比前后必须 ≥ 5 seed 看均值，单局差异不能当结论 |

---

## 8. 调参记录

（每轮追加：日期 · 改了哪一层 · 前后指标 · 结论）

- 2026-09-25 P5.0：搭框架，未改任何数值（`base` / `balance.json` 与代码默认值一致）。
- **基线**（`build/balance/baseline_*_0925_1403`）：8 名单人开局与 6 个预设编队**全部在 3:20–6:10 团灭**，胜率 0；死因 bullet / contact_ripper / corrode，死时灯火多为 0–30（熄灯死亡螺旋）。`--nodeath` 下全员 10:00 达 Lv32、精二占比 33–66%、伤害构成合理 → **输出层没问题，问题全在生存层**。另发现旧截断 620 s 只给最终 Boss 20 秒，改为 780 s。
- 机器人：躲弹从「90px 内小幅侧移」改为「170px 内按弹道预判闪避」、远离远程怪射程、低血量远离敌群重心；`--botrandom` 对照显示选卡策略不是死因（随机选也在同一时间死）。
- **r1**（敌人层 + 灯火层）：敌人攻击曲线 dmg_div 260→420；灯火受击损失 (4+30·dmg/HP)→(3+18·dmg/HP)；博士回复 0.6→1.0；大群规模 24+t/9 → 20+t/12；ripper 接触伤害 16→12。编队平均存活 4:30→5:30。
- **r2**：侵蚀池系数 2.0→1.2（一记带侵蚀的攻击原本等于再补 60% 伤害）。编队存活 4:50–7:50；`--nodeath` 下胜率 33–100%、托底 1.3–9 次 / 局（首次约 9:00，说明后期爆发伤害仍偏高）。
- **r3**：敌人生命曲线 hp_div 120→150（5:00 ×3.0 而非 ×3.5）；博士基础生命 100→120。编队存活 5:20–7:03、单人 5:05–6:40，仍 0 胜；死因 contact_ripper / bullet 各半。**博士 +20% 生命几乎没改变死亡时间** → 机器人在 5–7 分钟被 ripper 贴身围死是走位问题，继续堆数值会把真人玩家的难度调没。此处暂停曲线调整，等真人对照数据（见 §9）。
- 铃兰单人开局稳定在 3:30 前死、Lv4、击杀 ≈ 280（其他人 ≈ 1500）：与机器人无关的硬短板，`base.atk` 16→20、`cd` 1.2→1.0。
- **2026-09-25 P5.1**（balance/p5 并入 main 之后，含医疗无人机）：
  - 基线 `after_drone_squads_0925_1530` / `after_drone_solo_0925_1532`（3 seed）：编队存活 4:44–8:15、单人 4:57–7:53，0 胜，与 r3 同一水平 → 无人机改治疗没有明显推高或拉低生存。
  - 铃兰单人 **3:21 / Lv5 / 击杀 295 / 从未招募**：r3 的 +25% 攻击毫无作用。逐局看：她到 1:10（Lv4）为止和别人同步，1:15 的第一波骨潮（26 只）清不动，之后经验断流，Lv5 要到 2:20（别人 1:20），死时还没招到人。根因不是面板而是**纯单体没有清群手段**，这是"任何职业都能开局"的结构性漏洞。
  - 改动（干员层，只动 suzuran）：狐火命中带小范围溅射 `base.aoe` 26（艾雅法拉 58 的 45%），`arcane` 弹种支持 `aoe` 字段；`hit_sources` 加 `area`。→ `p51_suzuran_splash`（5 seed）：**5:48（最短 4:28）/ 击杀 2122 / 1:15 到 Lv5 / 每局招到 2 人**，落在全员中游（4:57–7:53），3:30 硬指标过关，最强 / 最弱存活比 2.35 → 1.36。
  - 回归 `p51_squads_0925_1538`：三个含铃兰的编队 8:15→8:03（出现首次 33% 胜率）、7:10→5:33、7:33→7:33，狐火占比 12–17% → 22–28%（增幅位上限 25%，贴边，先不再加）。不含铃兰的编队也动了 ±1–3 分钟 → 见下条，3 seed 的噪声就有这么大，护栏"胜率变化 ≤ 25 点"在 5 seed 以上才有意义。
  - 新增统计：BALANCE 输出 `heal`（有效治疗按来源）与 `drone`（无人机等级），批跑表多一列「治疗来源」。结果：没带医疗干员时无人机占回复的 47–60%，带了塞雷娅 / 凯尔希 / 水月时降到 7–14% → 「保底治疗、不是主力」成立，`drone_chance` 0.18 不动。
  - **复现性**：同一 `--seed` 两次结果不同。原因之一：`Sfx.play()` 按墙钟限流后用全局 `randf_range` 调音高，全局随机流被消耗的次数随真实耗时变化，而升级卡池的 `shuffle()` 用的就是全局流。已改为音效私有 RNG，之后同 seed 到 3:40 逐秒一致，3:50 后仍会分叉（未定位，见 §7）。

- **2026-09-25 第二批干员接入（架构会话）**：乌尔比安 / 艾丽妮 / 归溟幽灵鲨 / Logos / 流明 按 §9 走了一遍（solo 3 seed + 两种编队 2 seed，`build/balance/b2*`）。发现两处与铃兰同类的结构性短板并在干员层修掉：Logos 纯单体 → 命中溅射；流明光弹绑在治疗节拍上 → 独立射击。数值只动各自 `base`，曲线未动；结论与表见 docs/26「第二批数值粗调」。`tools/balance_run.py OPS` 已加五人，新增 `batch2 / batch2solo` 预设。

- **2026-09-26 曲线 r7（用户反馈「前期简单、后期容易暴毙」，主控模式 + 可见成长 + 技能充能修复之后）**：
  - 结构问题：`enemy_ai.gd` 石头怪子弹伤害 `5 × (1 + t/300)` 不封顶（10 分钟 ×3），而其他敌人攻击 8 分钟封顶 → 改为 `min(t, 480)`。
  - `balance.json enemy`：`spawn_base` 1.6→2.3、`spawn_div` 35→40（前期敌人多、后期总量略少）、`first_elite` 90→60、`hp_div` 150→130（前期血量爬得快）、`hp_late_div` 300→420（8 分钟后放缓）、`bullet_dmg_mult` 0.85→0.75。
  - 高手机器人满编（`curve_base` → `curve_r1`）：胜率 83%→78%，死亡仍集中在 9–10 分钟（终局 Boss 阶段）；普通机器人单人开局（`curve_r1_solo`）：存活 4:31–11:00，艾雅法拉单人开局 3:30 存活掉到 50%。
  - 结论：机器人对「前期简不简单」不敏感（高手几乎不掉血），终局压力的主因需要真人对照确认；下一步看真人反馈再动终局 Boss。

- **2026-09-26 藏品层：流派重做（docs/35）**：实装 86 → 133 件，8 条流派 A–H，职业专属藏品按编队出现。
  - 对照（同一份干员代码，7 开局 × 8 seed × 高手 / 普通，`relic_before2` / `relic_r3`）：高手胜率 60% → 64%、存活 9:30 → 9:43；普通 5% → 7%、7:06 → 7:34；藏品直接伤害 1.7% → 4.4%（倍率类不计入）；无效拿取 0。
  - 首轮暴露两件事：守护·续航吃流派加权后越出越多（候选占比 31%）→ 不参与加权、时间权重 0.7 → 1.0；批跑工具只查 stdout 漏报了水月的脚本错误 → 改为也查 stderr。
  - 旁证：普通机器人胜率在没有藏品改动的两次基线之间从 24% 降到 5%，发生在其他会话的干员提交之间（二分见 docs/35 落地记录），调干员时留意。

- **2026-09-26 干员层：主控回复分档 + 输出往预算拉（用户要求「乌尔比安这类回血上调、各干员数值均衡」）**：
  - 主控回复：见 docs/23（没有自疗的近战 1.4–1.7，远程与小额自疗 1.1，治疗型 1.0）。
  - 基线问题（`solo × 高手 × 12 seed`，每名干员当开局主控、自然招募）：主控胜率 16–91%，流明 16%、塞雷娅 33%、艾丽妮 33% 垫底；
    输出（`opcmp` 满 10 分钟）逻各斯是主输出均值的 162%、维什戴尔 142%、艾雅法拉 121%，斯卡蒂 56%、幽灵鲨 68%、艾丽妮 70%、乌尔比安 72%。
  - 改动（`base` 段，代码缺省值同步）：逻各斯 atk 40 → 29；维什戴尔 atk 34 → 30；艾雅法拉 熔岩天降 50% → 30%（上一轮 N5 改动让它翻倍）；
    乌尔比安 atk 42 → 52、间隔 1.5 → 1.4；艾丽妮 atk 18 → 24；幽灵鲨 atk 34 → 46；斯卡蒂 atk 26 → 40、间隔 0.75 → 0.7；凯尔希 Mon3tr 22 → 25；
    流明光弹 16 → 22；铃兰 atk 20 → 26；塞雷娅 atk 28 → 40、注射器 60% → 80%、碎晶 50% → 60%、拳击触及 70 → 92、间隔 1.1 → 0.9。
  - 结果（同 12 seed）：高手主控胜率 68% → 76%、存活 9:36 → 9:52；流明 16% → 75%、艾丽妮 33% → 58%、乌尔比安 75% → 100%、斯卡蒂 66% → 75%；普通 8% → 10%、存活 7:20 → 7:41。
    塞雷娅第一轮只加攻击反而 33% → 16%：她当主控时半数局 5:00 还在 3–5 级、一个人都没招到——单体出拳清不动开局，怪堆到上限 450 只，经验球捡不到。
    根因是阻挡圈把怪推到 62 以外、拳只够到 70；改触及 92、间隔 0.9 后单测 12 seed：高手 11 / 12 胜、开局卡死 0 局、60 秒击杀 27–83 → 92–144。
  - 铃兰这一轮 83% → 50%（12 seed）：输的局多是近战队友为主的编队，她自身输出低、靠招募；远程主输出这轮被削后，辅助类主控更吃招募运气。下一轮先看她。
  - **结构性问题（数值调不动，待定）**：固定队友（推进之王 + 水月）下，远程 AoE 三人（逻各斯 / 艾雅法拉 / 维什戴尔）拿走 73–78% 的伤害，近战主输出只有 20–32%。
    射程决定谁先摸到怪：远程在怪走到近战面前之前就清掉了，所以大幅改近战数值只让占比挪几个点（本轮艾丽妮 24% → 32%、斯卡蒂 16% → 20%）。
    要真正拉平得改机制（例如近战普攻的触及 / 范围、远程的射程），需要用户拍板。
  - 测法：`opcmp` 的「每分钟伤害」在怪被清空时封顶，而且队友一改动所有人的数字都跟着变，只适合看同一版本内的排名；前后对比用新预设 `share`（被测干员 + 推进之王 + 水月，队友不在改动里）。

- **2026-09-26 近战触及加大、远程射程略缩（用户定：拉平近战与远程的差距，快速验证不跑全量）**：
  - 改动（`base`，代码缺省值同步）：近战普攻触及约 +50%——乌尔比安 70 → 105、斯卡蒂 88 → 132、推进之王 72 → 108、塞雷娅 92 → 115、幽灵鲨 80 → 120、
    水月挥伞半径 95 → 142、艾丽妮穿刺长 120 → 180、凯尔希 Mon3tr 62 → 93；远程三名主输出射程 −10~12%——维什戴尔 480 → 420（出手 / 技能索敌同比例）、
    逻各斯 420 → 370、艾雅法拉 380 → 340（出手 / 技能索敌改为跟着射程走）。铃兰、流明是辅助，不在差距里，射程不动。
  - 快速验证（新预设 `gap`：近战主控 + 远程主输出 + 辅助 五组，4 seed，`--nodeath --maxt=480`，同 seed 对照）：
    | 方案 | 近战主控占伤害 | 远程主输出占伤害 |
    | --- | --- | --- |
    | 原值 | 19% | 62% |
    | 近战 +20~30%、远程 −10~12% | 21% | 62% |
    | **近战 +50%、远程 −10~12%（采用）** | **28%** | **53%** |
    | 近战 +50%、远程 −25% | 28% | 57% |
    远程射程再砍没有额外收益，拉平靠近战触及。主控可玩性快检（预设 `gapleaders`，6 名主控 × 3 seed）：改前 12 / 18 胜，改后 18 / 18（艾丽妮 1 / 3 → 3 / 3），远程主控没有变弱。
  - 留意：3 seed 的快检里六名主控全胜，整体可能偏易；下次全量矩阵时核一下高手胜率是否超出 70–95%。

## 9. 新增干员的数值流程（防止加人就崩）

每加一名干员都走同一条流水线；不走完不进池。目标：新干员进来只改她自己的文件，不碰曲线、不碰别人。

### 9.1 先填预算表（写进 docs/26 的干员条目）

| 项目 | 填什么 | 约束（§3.3） |
|---|---|---|
| 职业角色 | 主输出 / 节奏 / 保护 / 治疗 / 增幅 | 决定直接 DPS 预算：100 / 85 / 70 / 65 / 65% |
| 直接 DPS | `atk / cd`（远程再乘命中率 ≈ 0.85，AoE 不加成） | 基准 25–35，按角色预算打折 |
| 补偿能力 | 治疗 %/秒、全队技力 秒/命中、减速 / 易伤 / 击退 | 补偿价值 ≈ 被打折掉的那部分 DPS |
| S1 / S2 / S3 | 充能秒数 + 效果 | S1 ≈ 3–5 秒普攻伤害，S2 ≈ 8–12 秒，S3 ≈ 15–25 秒或 8–14 秒形态 |
| 天赋（精一） | 一条被动 | 相当于 +10–20% 效率，不再是第二个技能 |
| 成长线 | 6 节点：+15% / +15% / 精一 / +15–20% / +20% / 精二 | 与其他人完全同构，不许有 +30% 的节点 |
| 精二条件 | 编队职业 / 博士被动 / 博士等级 | 与已有干员不重复太多，避免总是同一套编队 |
| 档位 | S+ / S / A+ / A | `balance.json operators` 只允许 0–5% |

### 9.2 数据落位

1. `data/characters/<id>.json`：`base` 段写全所有数字（攻击、间隔、射程、范围、各技能倍率、天赋系数），脚本里**只允许**用 `base("key", 默认值)` 取数，默认值必须等于 JSON 里的值。
2. `hit_sources`：每个伤害来源都登记（emitter / origin / range / kind / tags），否则批跑的伤害占比和藏品 Tag 加成都对不上。
3. `balance.json operators.<id>`：先写 0，档位系数留到 9.4。
4. `tools/balance_run.py OPS` 加上 id（solo / pairs 预设自动覆盖）。

### 9.3 校验（提交前必跑）

```bash
python tools/check.py                                             # 快检（docs/36）：契约 + 每名干员冒烟 + 同 seed 复现
python tools/check.py --ab main                                   # 与 main 同 seed 对比机器人标准矩阵
python tools/balance_run.py --op <id> --seeds 5                   # 单人开局
python tools/balance_run.py --squad <id>,wisadel,saria --seeds 3  # 带一个强输出 + 一个保护
python tools/balance_run.py --squad <id>,suzuran,kaltsit --seeds 3
python tools/balance_run.py --preset squads --seeds 3             # 回归：老编队不许变
```

通过标准（§1.1 的子集）：

| 检查 | 标准 |
|---|---|
| 单人 3:30 存活率 | 与同角色的现有干员相差 ≤ 15 个百分点（例如新术师对艾雅法拉） |
| 三人队里的伤害占比 | 主输出 30–50%；节奏 20–35%；保护 / 治疗 / 增幅 10–25% |
| 击杀 / 分钟（`--nodeath`） | 与同角色现有干员相差 ≤ 20% |
| 老编队回归 | 新干员不在其中的编队，同 seed 结果应逐局完全相同（2026-09-26 起可复现，docs/36 §3）；不同就说明改动影响到了别处 |
| 无人机 / 藏品占比 | 仍在 10–20% |

### 9.4 定档

校验通过后才写 `balance.json operators.<id>`：想让她「强一点」就 +3–5%，想让她「基准」就 0。**不许用档位系数救数值**——如果 +5% 都不够，回 9.1 改预算表。

### 9.5 不许做的事

- 不改 `balance.json enemy / xp / levelup` 来适配某个新干员。
- 不给新干员第四个技能、第七个成长节点、或 >20% 的单节点。
- 不新增只对她生效的全局属性（`stat_defs`）——用 `op:<id>` 作用域。
- 不在 .gd 里写裸数字（`34.0`）；写 `base("atk", 34.0)`。

## 10. 待办：真人对照

> 2026-09-25：先用 docs/29 的四档机器人近似（AFK / 手残 / 普通 / 高手），基线与结论见 docs/29 §5。

机器人是「会躲弹、会拉开远程怪、低血量后撤」的中等水平玩家，但它不会绕着怪群跑圈、不会主动找空地。5–7 分钟的 ripper 围死很可能是真人不会犯的错。需要一组真人数据校准（同一版本、难度 0、随便选 3 人）：死亡时间 / 通关与否、死因、最后一次看到灯火的值。有了它才能决定 §4 的曲线是否继续放缓。
