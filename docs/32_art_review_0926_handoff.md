# 编队人物返修交接 · 2026-09-26

对应 `docs/32_squad_art_review.md` §1、§2.1。开始时 `codex/art` 工作树干净，已执行 `git merge main`，合并点 `4125ca4`；已包含请求中 `21482b3` 的审查文档。没有改动 §3、`game/` 或运行时逻辑。本次灯塔修复由用户对 §1 的明确授权覆盖。

## 交付与接入

- `art/incoming/`：118 张透明 PNG 帧条，共 618 个导出帧（两种密度合计）。其中 106 张为既有素材返修，12 张为六条新增动作的两种密度。
- 人形主版本 96×96，脚底锚点 (48,92)；fallback 48×48，锚点 (24,46)。Mon3tr 和灯塔沿用 128/64px，逻辑锚点 (64,120)/(32,60)；Mon3tr 保持悬浮，不把本体底端强贴地。
- `art_review_0926_manifest.json` 是本次同名文件的最新元数据和 SHA256，优先于旧批次、posture、mon3tr manifest 中这些文件的旧哈希。未列出的素材仍用各自原 manifest。
- 旧动作保留文件名、帧数、FPS、事件帧、循环/结束行为；此次不是代码接入。左右镜像、特效挂点和 Godot 实机检查由 Claude 接续。
- 主版本重绘并导出原生细节；fallback 最近邻采样、重置一像素边缘和脚底。每个角色跨动作、跨两种密度共用至多 48 色，透明度只有 0/255。

## §1 逐项返修

| 对象 | 本轮处理 |
|---|---|
| 第四批五人 | 所有高清动作外缘扩展并统一为 `#080E18` 2px 描边；48px 为 1px。清理背景残留，独立检查 R=B/G=12 暗洋红为零。对冷色衣饰和中间色高光提亮，保留人物原配色与自然头颈连接。 |
| 维什戴尔 | 普攻 f2 加入抬口后坐与后压；枪体用清晰金属块面和炮口表达，减少锯齿般碎白噪点；技能平射阶段保持相近枪体尺度。沿用腰间平射，未强改肩扛动作。 |
| 推进之王 | 增强锤头体积；普攻重新组装为肩后准备→举锤→向前下挥→前方收锤，取消结束帧锤子直接穿到身后。 |
| 斯卡蒂 | 恢复黑色帽面；单独返修四帧待机，统一帽高、剑角和发尾毛球。普攻保留长剑横向扫出的轮廓；新增完整回旋动作单独交付。 |
| 塞雷娅 | 强化盾牌橙边；待机四帧统一左侧灰发与橙色发束。原举盾技能保留，治疗改用新文件。 |
| 艾雅法拉 | 跑动按脚下根位置重新对齐，取消旧帧条的大幅横移；待机仅一帧闭眼；杖身加粗并加入木色亮边。 |
| 凯尔希 | 重整全部动作的白发与外套色块，去掉棋盘状碎块；新增伸手指挥动作，区别于持药治疗。 |
| Mon3tr | 在最新已确认的无脚、无持械装甲造型上统一头向前的剪影和红眼；普攻保留同一头部与躯干，不再切回另一套竖直尖刺团。新增绿色裂隙增强的熔毁姿态。 |
| 铃兰 | 收紧日常持杖轮廓；skill f5 直接使用本轮 idle f0，睁眼、杖头回到左上，避免切待机时跳变。 |
| 乌尔比安 | 锚杆、回钩锚爪及金色舵轮从大衣中分离；简化大衣噪点。skill f1–f2 后仰持锚，f3 才离手；整条保持一致缩放，锚不裁边。 |
| 艾丽妮 | 加强粉色内衬和暖色提灯，每帧保留灯；skill f2 蓄势转身、f3 背侧向大横斩，普攻仍为刺击。腰间小枪没有额外放大。 |
| 归溟幽灵鲨 | 锯柄加长，强化金色机械齿轮和尾端尖刺；普攻 f2 转为背身回旋而非直推。修女替身保持胸前合手、小幅摆动，头纱轮廓与脸部对比更稳定。 |
| Logos | 清除洋红边缘，头发改为更深的冷灰；出手动作让浅色骨笔尖可辨，与黑色握柄区分。保留已修正的头颈姿态。 |
| 流明 | 所有待机/跑动帧均有分离于旅行包的暖色铜灯；补可见伞柄和收拢的背伞。attack f1 低灯，f2 举到高位且玻璃最亮；统一灯体规格并修正跑动根位置。 |
| 灯塔 | 两帧复用同一塔身；仅灯室玻璃亮度与暖色改变，塔墙/门/屋顶不会随机翻动。 |

## §2.1 六条独立动作

以下文件名均同时交 `.png`、`@2x.png`。事件帧从 0 开始。

| 文件基名 | 帧数 | FPS | 事件帧 | 建议接入 |
|---|---:|---:|---:|---|
| `op_saria_skill_heal` | 6 | 12 | 2 | S1/S2 抬手给药，盾牌保持低位；原 skill 留给 S3。 |
| `op_siege_command` | 6 | 12 | 2 | S1/S3 举锤号令；原 skill 仍为 S2 砸地。 |
| `op_skadi_attack_spin` | 6 | 12 | 2 | S3 绕身回旋，含背身阶段；原 skill 保留重斩。 |
| `op_mon3tr_skill` | 6 | 12 | 2 | 起手 f0–f2；8 秒持续阶段建议局部循环 f2–f4，退出 f5。不要把六帧整条误当 8 秒的动画。 |
| `op_kaltsit_command` | 6 | 12 | 2 | 向前伸手指挥 Mon3tr，不持药剂做治疗。 |
| `op_specter_unchained_fall` | 5 | 10 | 4 | 不循环，播完保持末帧，再切 doll。f0 精确复用本体 idle f0，f4 精确复用替身 doll f0。 |

Saria/Siege/Skadi/Kaltsit 四条新增动作的最后一帧复用各自 idle f0，便于自然回到待机。新动作未自动连接技能状态机，§2.2 次要新增项不在本轮交付范围。

## 验证与预览

- `art_review_0926_ground_qa.json`：110 张地面素材 / 578 帧，检查尺寸、帧数、二值 alpha、描边、画框留白、精确脚底、色数、逐帧非重复、事件索引及 SHA256。
- `art_review_0926_float_qa.json`：8 张 Mon3tr / 40 帧，同样检查，因其悬浮只跳过“可见底端等于地面锚点”。
- `art_review_0926_style_qa.json`：118 张全部检查；人形高清两层外缘均为描边色，暗洋红残留为零；灯塔灯室外变化像素为零。
- `art_review_0926_outline_compare.png`：第四批旧/新待机横向对照；`art_review_0926_<角色>.png`：角色全动作预览。
- `art_review_0926_preview.html`：离线自包含预览，支持两种密度、播放/暂停/逐帧、背景颜色与锚点显示。
- `art_review_0926_provenance.json`：内置 image_gen 提示词、源图路径和哈希、机械导出脚本哈希。原始高分辨率生成稿和脚本保留本机 `E:/水月/squad_review_20260926/`，不作为运行资源入库。

**尚未进行 Godot 接入与实机验证。** docs/32 §3 挂点来自旧稿；本次灯、锚、笔和挥击姿势有变化，Claude 接入时请对照新的事件帧重新取挂点，不直接把旧坐标当成已验证值。人物仍以右向设计，左右镜像和战斗遮挡需要在游戏里确认。

按 docs/06：从干净 main 查看并 cherry-pick 本次美术提交。无需 cherry-pick 起始 merge 提交，也不要整分支强制覆盖 main。
