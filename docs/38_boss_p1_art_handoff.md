# Boss 重做 P1：最后的骑士 / 伊莎玛拉动画美术交接

依据：`docs/38_boss_p1_art_acceptance.md` §2、`docs/38_boss_redesign.md` §6.2、`docs/06_git_collaboration.md`。本次返修六条 Boss 人物帧条、预览、清单与本说明；没有修改游戏代码。

| 文件前缀 | 单帧尺寸 | 帧数 | 播放建议 | 动作 |
|---|---:|---:|---|---|
| `e_knight_plant` | 96×80 | 4 | 一次 | 举枪、落枪、枪尖触地与少量冰屑；独立冰墙由程序绘制 |
| `e_knight_charge_form` | 96×80 | 4 | 循环 | 骑士压低、枪平举，罗辛南特四相奔跑 |
| `e_ishar_transform` | 112×96 | 6 | 一次 | 人形、白壳包覆的半变身、逐渐卷曲、完整白壳金棘海嗣 |
| `e_ishar_t` | 112×96 | 2 | 循环 | 白壳金棘海嗣悬浮待机，甲壳和棘鳍轻微摆动 |
| `e_ishar_t_move` | 112×96 | 4 | 循环 | 卷曲、伸展、前滑、收回的悬浮移动 |
| `e_ishar_t_attack` | 112×96 | 4 | 一次 | 蓄力、张开青色裂纹、头部前冲、收回 |

每条帧条都有同名 `@2x.png`（每帧宽高翻倍）。图片位于 `art/incoming/` 根目录，横向均分，朝右，透明 RGBA，二值 alpha，底版 **1px** `#080E18` 轮廓描边，高清版 **2px**。所有帧在自身画格内，横向居中、底部对齐，向上的甲壳和棘鳍利用新增高度。变身前两帧的可见像素位于居中 80×80 区域，变身末帧的 103×88 外框直接接待机的 103×88。`e_ishar_t.png` 是待机条名，无 `_idle` 后缀。

**代码侧接入注意**：伊莎玛拉本体 `e_ishar.png` 仍是 80×80；本次四条变身相关帧条必须以单帧 **112×96**（@2x 为 **224×192**）切片，不能继续从本体宽度 80 推算。底部锚点应保持一致，绘制时不要按新画格中心垂直对齐。骑士两条保持 96×80，无需更改。

伊莎玛拉变身形态按用户提供的实机截图：卷曲的白灰色分节甲壳、金色钩状棘鳍、向右下方的尖甲壳头、青色锯齿裂纹。**变身后不是人形**。本轮将第二帧改为面部仍可见、白壳已包覆的中间态。待机、移动、攻击与变身末帧体量统一：1 倍图待机外框 103×88，移动各帧 103×88，攻击普通帧 103×88、前冲帧 107×88，变身末帧 103×88。攻击前冲帧仅有本体动作与原有青色裂纹，不含独立青色冲击特效。

预览：`art/incoming/boss_p1_knight_preview.png`、`art/incoming/boss_p1_ishar_preview.png`，深蓝背景按 @2x 尺寸展示；`art/incoming/boss_p1_manifest.json` 记录尺寸、帧数与每张 PNG 的 SHA-256；`art/incoming/boss_p1_qa.json` 记录逐帧外框、非透明像素计数、紫色残留与描边宽度。

导出后核验 12 条帧条：确切尺寸、帧数、SHA-256、二值 alpha、紫色残留像素 **0**、1px／2px 边界描边、逐帧外框与透明画布边缘。对照验收反馈，伊莎玛拉出手不再缩小，骑士 `_plant` 后两帧仅留枪尖触地的小冰屑，不含冰墙。目视检查了两张深海背景预览。此交付没有进行游戏内接入或 Godot 播放测试。
