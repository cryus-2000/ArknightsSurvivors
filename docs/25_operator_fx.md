# 25 · 编队干员特效设计（Claude，2026-09-25）

> 分工（AGENTS.md）：人物形象归 Codex，攻击 / 命中 / 技能特效归 Claude。本文是首批 7 名干员 + Mon3tr 的特效设计与实现记录。
> 原则：**每个干员的特效取自她在《明日方舟》里的技能表现**（配色、形状、节奏），再按本作像素风与"3–4 人同屏可读"的要求收敛；所有特效程序绘制，不占 Codex 的帧条。

## 0. 实现方式

- 每个干员脚本持有自己的粒子表 `pfx`（`character.gd` 的 `fx()` / `fx_sparks()`），`squad.gd` 每帧 `_tick_pfx()`，地面层在 `draw_entities_floor` 后、身上层在 `_draw_skill_over` 后绘制。
- 通用粒子 kind：`ring`（扩散环）、`spark`、`glow`、`shard`、`line`、`flame`、`mote`、`crack`（地裂）。干员专属 kind 在各自 `_draw_pfx()` 里画（表中标 ★）。
- 近战命中走 `melee_hit()` → `_hit_fx(e, origin)` 钩子；子弹可带 `src`（伤害来源）、`fx_col`（爆炸 / 拖尾颜色）、`hidden`（不画默认弹体，干员自己画）。
- 干员主色：`character.gd CLASS_COL` 按职业给默认，JSON `"col": [r, g, b]` 可覆盖；HUD 环、卡面、默认粒子都用它。

## 1. 逐人设计

| 干员 | 原作依据 | 本作特效 |
|---|---|---|
| **维什戴尔** 狙击 | 炮击为范围物理；天赋「残影」：被冲击波波及的残影有概率爆炸并眩晕；整体黑红、黑烟 | 主色暗红 `(0.95, 0.22, 0.2)`。炮弹：黑色弹体 + 暗红火星尾，抛物线；落点前 0.15 秒地面出现暗红落点环。落点：暗红闪光 + **黑烟团**（深灰半透明圆膨胀淡出）+ 红色冲击环 + 黑色碎片。余震：地面红色双环 + 短裂纹 + 三缕上飘的黑色雾丝 ★`wisp`。殉爆（残影）：击杀处先浮起**黑色人形残影**（黑椭圆 + 一对红点，0.25 秒）★`shade`，随后红黑爆裂并晕眩 0.6 秒。饱和炮击：起手炮口暗红光晕 + 后坐火星，落点环更大 |
| **艾雅法拉** 术师 | S2「火山」：范围扩大、向随机目标投熔岩火球；点燃；橙红白 | 主色熔岩橙 `(1.0, 0.5, 0.2)`。火山弹：自绘熔岩球（橙核白心）+ 火星尾 ★`lava_orb`；爆炸：橙白核心、暗红环、迸溅熔岩珠（带重力）。火山：技能期间脚下橙色热浪环呼吸；每次喷发：地面红裂纹 0.12 秒 → **熔岩柱** ★`lava_pillar`（底宽上窄，白芯，顶端飞溅岩浆珠）→ 熔岩池（地面暗红椭圆 + 上升的橙色气泡）。点燃：敌人身上跟随的火舌 ★`burn` |
| **凯尔希 / Mon3tr** 医疗 | Mon3tr 绿色荧光；S3「熔毁」真实伤害、ATK 随时间衰减；治疗单元 | 主色荧光绿 `(0.55, 1.0, 0.5)`。爪击：**三道平行绿色爪痕** ★`claw`（略弧、随时间拉长淡出）+ 绿色火花；命中敌人处绿色划痕闪。狂暴：Mon3tr 周身绿色脉冲环 + 上升绿光点 + **残影**（上一帧位置的半透明绿色本体）。熔毁：绿白核心闪光 + 三重扩散环 + 放射光线 + 地面绿色裂纹。治疗：绿色十字上升 + 凯尔希→博士光束（沿用）；技能起手凯尔希身边绿色医疗环 |
| **铃兰** 辅助 | 狐火（金色火焰）；S3「Foxfire Haze」：金色区域，减速 + 脆弱，范围内友军回复 | 主色狐火金 `(1.0, 0.82, 0.45)`。光域：地面淡金填充椭圆 + 边缘 **6 团绕行狐火**（火舌，按 g.t 旋转）；光域内飘浮的金色光点上升。狐火弹：自绘金白火球 + 火舌尾 ★`foxfire`；命中：金色小爆 + 4 火星。光域技能：起手金色放射线 + 扩散环；持续期间**博士每秒回复 0.5% 生命**（对应原作回复友军） |
| **推进之王** 先锋 | S2「跃空锤」跃起空中转一圈、砸地重击相邻敌人并回 DP；S3 眩晕；狮王金 | 主色狮王金 `(1.0, 0.78, 0.35)`。锤击：金色重弧 + 命中点十字重击闪光 + 灰色尘土上升；命中后**金色技力粒子从她飞向每名队友** ★`sp_mote`（对应回 DP）。震地：双重金色冲击环 + 地裂 + 抛起的尘柱 + 震屏 + 更多技力粒子 |
| **斯卡蒂** 近卫 | 深海猎人：深蓝黑 + 白浪；精二红瞳；S3 大范围扫击 | 主色深海蓝 `(0.35, 0.55, 0.95)`。横扫：深蓝弧光 + 窄白边（两层斩击）+ 浅青水珠飞溅（带重力）。反手斩：更亮的白青。重斩：抬剑时**眼位一点红光**，下劈时巨大深蓝新月 + 白色浪沫边 + 地面深蓝裂纹 + 水花柱（大量浅青水珠上抛） |
| **塞雷娅** 重装 | S3「钙质化」：琥珀色晶体区域，减速 + 脆弱 + 回复；盾 | 主色琥珀 `(1.0, 0.72, 0.38)`。阻挡圈：地面琥珀色**分段虚线环**（缓慢旋转）+ 3 枚绕行的小晶体；被推开的敌人脚下琥珀小闪。盾击：琥珀短弧 + 推力直线。钙质化：博士周围**升起 8 根琥珀晶柱** ★`crystal`（菱形柱从地面长出、停留、淡出）+ 金色环 + 琥珀色治疗十字；精二再加冲击波与地裂 |

## 2. 收敛规则

- 同屏最多 4 名干员：地面层特效（光域、阻挡圈、熔岩）用低 alpha（≤ 0.3）填充 + 细边，避免叠成一片。
- 亮度 > 1 的颜色只用于核心闪光（≤ 0.2 秒），让辉光只抓住"出手瞬间"。
- 每次普攻的粒子数 ≤ 12，技能 ≤ 60；粒子寿命 ≤ 0.6 秒（地面残留物除外：熔岩 3 秒、晶柱 1.5 秒）。
- 命中反馈仍由 `_damage()` 的敌人闪白 / 挤压承担，干员特效只负责"是谁打的、打的是哪种伤害"。

## 3. 第三方素材接入（2026-09-25）

`tools/fx_import.py` 把 `art/third_party/`（不入库，见其 README）里的开放许可素材切帧、最近邻缩放、按干员色板做亮度分档重调色，输出到 `art/incoming/`（入库）并登记 `V6_FRAMES`：

| 帧条 | 来源 | 用在 |
|---|---|---|
| `fx_holy_pillar` / `_amber` | Pimen Holy VFX 02（16 帧 48px） | 铃兰 暖光 / 迷雾（中心 + 边缘六根，迷雾期间随机落柱）；塞雷娅 钙质化（琥珀） |
| `fx_holy_impact` | Pimen Holy VFX 01 Impact | 治疗命中星光（塞雷娅琥珀、凯尔希染绿） |
| `fx_heal_aura_green` / `_amber`、`fx_circle_gold` / `_amber`、`fx_shield_amber` | Ninja Adventure Magic/Aura、Circle、Shield | 治疗光环、法阵、药剂散布护体 |
| `fx_flam_hit`、`fx_sunburst`、`proj_lavaball` | Ninja Flam、CodeManu sunburn、OGA Fireball | 艾雅法拉 火山弹命中 / 喷发、熔岩池翻滚、弹体 |
| `fx_claw_green` / `_double`、`fx_felspell` | Ninja Claw / ClawDouble、CodeManu felspell | Mon3tr 爪击（协同 / 熔毁用双爪）、熔毁 |
| `fx_slash_arc_deep` / `_heavy` / `_circle`、`fx_water_splash` | Ninja Slash Arc / Slash01 / Circular、Water | 斯卡蒂 横扫 / 重斩 / 潮汐、水花 |
| `fx_rock_burst`、`fx_rock_spike` | Ninja Rock / RockSpike | 推进之王 跃空锤碎石（2026-09-25 已删，改火焰 + 光柱） |
| `proj_foxfire` | OGA Light Bolt | 铃兰 狐火弹 |

维什戴尔暂不改（另一会话在做）。缺图时各处仍退回程序绘制。`_fx_sprite()` 新增 flip / 底部锚点 / 染色参数。

## 3.1 实战审查后的打磨（2026-09-25）

- **像素密度**：帧条特效统一封顶 3.2 倍放大，斩击 / 爪痕再 ×0.7、封顶 3 倍（`game.gd _blade_scale / FX_SCALE_MAX`）；原来按命中半径放大到 4–6 倍，颗粒比人物粗一倍。
- **修素材**：`fx_slash_circle_*` 源图 6 帧 × 63px 被按 7 帧切，圆环劈成两半、一帧只剩整块白扇面 → 重切。
- **去掉暖黄尘土**（用户：与深海不符）：干员移动扬尘删除；推进之王锤击黄土 / 橙色碎石改金色火星；乌尔比安、斯卡蒂不再用 `fx_water_splash`（前几帧是米白烟团），改程序深海蓝水珠。
- **乌尔比安**：锚改成他原画那把黑蓝钩锚（程序绘制过渡）；S1 / S3 改为「锚带锁链掷出 → 咬地顿帧 → 人顺锁链弹射（残影 + 速度线）→ 落地砸击」。
- **艾丽妮**：普攻从弧光帧条改为刺击光束（细长尖头光束 + 剑尖星闪 + 速度线，两段左右错开）。S2 / S3 本身是斩击，保留。
- **归溟幽灵鲨**：普攻从斩击环改为锯齿圆环（地面透视压扁、高速旋转，锯过的敌人沿切线甩冷白火星；S3 血红、齿更大）。
- **追踪弹**（铃兰狐火）：匀速只转方向，不再越绕越慢。
- 以上三人的程序版都是过渡，手绘帧条需求见 docs/30。

## 5. 可见成长与三档特效（2026-09-26，用户确认）

### 5.1 规则

1. **截图可辨**：同一干员在「精零 N1 N2 后 / 精一 N4 N5 后 / 精二」三个阶段，不看 UI 也能分辨。用 `--compareshot=<id>` 出三联图自查。
2. **成长节点只做看得见的变化**：N1 / N2 让普攻的核心元素变多或变形；N4 / N5 强化一个已有技能（次数、发数、段数、可见的形态）；精一 = 解锁 S2 + 天赋；精二 = 解锁 S3 + **普攻质变**。持续时间、充能这类看不见的改动不单独占节点。
   **选下当场就要看得到**（2026-09-26 用户反馈后补）：节点只能强化此时已解锁的技能（N4 / N5 只能强化 S1 / S2，S3 精二才有）。想让它连带强化 S3，写成「……；精二后「S3」同样如此」。精英化解锁的技能立即充满，选下就放。`tests/test_core.gd` 检查说明里有没有引用未解锁的技能，`python tools/check.py` 检查每个节点选下当场有没有变化。
3. **不重复**：N1 / N2 与精二质变不得复刻任何技能或天赋；N4 / N5 不加新机制。
4. **不 OOC**：每条变化都要有原作依据（docs/31：技能、天赋、档案）。
5. **不放大、不震屏**：主控只多一圈脚下标记；爽感来自元素变多、变形。
6. **数值守恒**：数量变多就下调单发，整体输出按 docs/27 预算。
7. **上限**：每名干员同时存在的弹体 / 召唤物 ≤ 12，地面残留 ≤ 6。
8. **第三方素材可以用在正式特效里**（用户确认），按 art/third_party/README 的许可署名。

### 5.2 逐人方案（2026-09-26 定稿：水月模板）

模板：**N1 · N2 普攻核心元素变多；N4 · N5 各强化一个已有技能并带新表现；精一 = S2 + 天赋；精二 = S3 + 普攻质变**。每个普通节点另外统一 **攻击 +6%**，身上的职业色气场光点随节点数变密（character.gd `advance()`，用户确认保留）。
命名按明日方舟习惯、各干员自成主题：水月用心理学术语，维什戴尔用「礼 / 余烬 / 亡魂」，乌尔比安用「必须……」式，艾丽妮用审判庭司法词，Logos 用语言学术语，流明 / 塞雷娅沿用原作技能天赋名。

| 干员 | N1 | N2 | N4 | N5 | 精二质变 |
|---|---|---|---|---|---|
| 水月 | 强迫性重复：触手 +1（打中的敌人不够时追向附近敌人） | 群体性癔症：触手 +1 | 惊跳反射：唤醒一击再钻出 2 根触手 | 病理性依恋：囚徒困境触手拖拽 | 固着：触手桩 1.5 秒 |
| 维什戴尔 | 余响：余震 +1 次 | 礼尚往来：一次打 2 个目标 | 余烬未冷：灰烬弹幕余震 3 圈并晕眩 | 亡者回响：凋零处刑炸到的敌人全挂残影 | 死魂灵的余息：2 个魂灵之影跟随挂残影 |
| 艾雅法拉 | 复咏：每次施法 2 发 | 火星迸溅：爆炸迸出 2 颗火星弹 | 星火燎原：点燃弹溅出 4 团小熔岩 | 熔岩天降：炽热强化弹炸开时再抛 2 颗熔融火球（精二后火山喷发同样） | 三重咏唱：3 发，后两发更大 |
| 凯尔希 | 骨爪增生：一击两爪 | 清创：一爪打 3 名 | 结构加固：医疗单元附 3 秒减伤护壳 | 不毁重构：医疗单元时 Mon3tr 小型三连爆并晕眩（精二后熔毁收尾同样） | 八面展开：前后同时出爪 |
| 铃兰 | 狐火点点：狐火 +1（常驻绕身） | 三火归一：每第 3 次三团合一 | 画地为牢：狐火连珠命中停顿 | 围炉：暖光后光域边缘狐火撞伤敌人 | 九尾祈愿：每次普攻都三团合一 |
| 推进之王 | 震地：落地冲击环 | 狮吼：冲击环扩大并击退 | 再跃：跃空锤连跳 2 次 | 万兽之王：冲锋号令释放号令波 | 破阵：每锤前方直线地裂 |
| 斯卡蒂 | 狂舞：连斩 2 下 | 异乡之舞：连斩 3 下 | 跃浪：重斩掀起前推海浪 | 涌潮：潮涌斩涌出水环（精二后潮汐期间每秒一圈） | 悲歌：每轮连斩后涌起小海浪 |
| 塞雷娅（拳击） | 钙质沉积：3 枚钙晶环绕 | 晶簇：5 枚 | 急救针剂：急救掷出 3 支注射器 | 碎晶：每 3 秒击碎一枚环绕钙晶，碎片飞向敌人（精二后钙质化期间击碎晶柱） | 莱茵充能护服：满格全方位冲击 |
| 乌尔比安 | 定点爆破：锚击第二段冲击 | 锁链回旋：每第 3 击锁链甩一圈 | 不容挣脱：必须接触拽 3 名敌人 | 通路洞开：必须接触的锁链裂开通路掀飞敌人（精二后必须开辟同样） | 血脉沸腾：击杀精英后下一击大爆破 |
| 艾丽妮 | 再审：刺击 3 段 | 严律：刺击更长更宽 | 卷风：疾风卷起周围小怪 | 二度裂潮：碎潮连放 2 次 | 追诉：刺中浮空敌人追加手炮 |
| 归溟幽灵鲨 | 双重回转：环斩 2 圈 | 血色潮痕：锯痕水迹 | 困兽：求生之技期间锯刃变红、半径 +50% | 阿戈尔挽歌：替身挽歌水纹、减速范围翻倍 | 无尽回旋：持续旋转，灯火越低越久 |
| Logos | 铭文：命中处咒文停留 1 秒 | 复指：同时攻击 2 个目标 | 转喻：提喻目标死后链接跳转 | 墓志铭：湮灭处决处写字爆开 | 众声喧哗：词法演化打 2 个目标 |
| 流明 | 发光单元：1 个 | 灯影成双：2 个 | 沐雨：净化之光洒下光雨 | 灯塔守望：净化之光后旋转光束 3 秒（精二后灯塔全程） | 灯火不灭：攒 8 发强化光弹 |

已删除（OOC 或与技能重复）：水月水刃（用户决定只长触手）、小 Mon3tr 影子、斯卡蒂回旋浪刃、铃兰狐火分裂、艾丽妮十字双刺、幽灵鲨回旋锯刃、塞雷娅晶片连锁，以及所有复刻 S3 / 天赋的「常驻缩小版」。

### 5.3 三档特效

每名干员的招牌攻击在精零 / 精一 / 精二用三套不同的特效（不同素材、不同形状），第三方帧动画按主色重调后叠加程序特效；精二加常驻光效（维什戴尔黑红光环、斯卡蒂红瞳水雾、铃兰九尾虚影、Mon3tr 荧光纹路等）。全局：敌人死亡特效按伤害类型区分，技能伤害数字用干员主色。

## 4. 待办

- 若某个特效在像素风下不够"实"，再补帧条（`fx_<op>_*.png`，走 V6_FRAMES）。
- 博士指挥技能（`doctor.json command`）的特效随技能一起做。
- 浊心斯卡蒂（第 4 批）：歌声光环（浅青 + 深红波纹）。
